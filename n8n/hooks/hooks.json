{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "python3 -c \"import os; exit(0 if all([os.getenv(\"N8N_API_URL\") and os.getenv(\"N8N_API_KEY\")]) else 1)\""
          }
        ]
      }
    ]
  }
}