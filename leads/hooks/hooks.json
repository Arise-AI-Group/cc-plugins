{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "python3 -c \"import os; exit(0 if all([os.getenv('APIFY_TOKEN')]) else 1)\""
          }
        ]
      }
    ]
  }
}