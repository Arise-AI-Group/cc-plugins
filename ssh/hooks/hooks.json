{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "python3 -c \"import os; exit(0 if all([os.getenv(\"SSH_KEY_PATH\") and os.getenv(\"SSH_PASSWORD\")]) else 1)\""
          }
        ]
      }
    ]
  }
}