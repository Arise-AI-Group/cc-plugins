{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "python3 -c \"import os; exit(0 if all([os.getenv('CLOUDFLARE_API_TOKEN') and os.getenv('CLOUDFLARE_ACCOUNT_ID') and os.getenv('DOKPLOY_URL') and os.getenv('DOKPLOY_API_KEY')]) else 1)\""
          }
        ]
      }
    ]
  }
}