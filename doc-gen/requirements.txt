weasyprint>=60.0
jinja2>=3.0
python-dotenv
pyyaml>=6.0

# XML processing for DOCX editing
lxml>=4.9

# PDF processing
pypdf>=4.0
pdfplumber>=0.10

# PDF rendering to images
pypdfium2>=4.0

# Document conversion (replaces Pandoc dependency)
python-docx>=1.0      # DOCX creation and parsing
mammoth>=1.6          # DOCX to HTML conversion
mistune>=3.0          # Markdown parsing
